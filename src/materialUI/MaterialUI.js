import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  name: {
    color: '#26A641',
    fontWeight: 'bold',
    fontSize: '7.5rem',
  },
  title: {
    color: '#26A641',
    fontWeight: 'bold',
    textAlign: 'center',
  },
});

export default useStyles;
